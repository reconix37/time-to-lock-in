{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the TTLI dashboard and mini-window",
  "windows": ["main", "mini"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
