-- TTLI v0.2.24 — приоритет категорий (перехват названий, фидбек Эдуарда).
-- 1 = перехватывает названия первой; чем меньше число, тем раньше категория
-- забирает совпадения. Дефолт 999 = «в конец очереди» (поведение существующих
-- категорий не меняется). Fresh-установки получают priority из обновлённого
-- 001_init.sql, поэтому апгрейд-ветка выполняется только при previous_version > 0.

ALTER TABLE categories ADD COLUMN priority INTEGER NOT NULL DEFAULT 999;

PRAGMA user_version=8;
