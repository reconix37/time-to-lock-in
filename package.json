{
  "name": "ttli-scaffold",
  "private": true,
  "version": "0.1.12",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test:icons": "node -e \"const fs=require('fs');const ts=fs.readFileSync('src/components/CategoryIcon.tsx','utf8').match(/CATEGORY_ICONS: Record<string, JSX\\.Element> = \\{([\\s\\S]*?)\\n\\};/)[1];const rs=fs.readFileSync('src-tauri/src/lib.rs','utf8').match(/const CATEGORY_ICONS: &\\[&str\\] = &\\[([\\s\\S]*?)\\n\\];/)[1];const front=[...ts.matchAll(/^  \\\"([^\\\"]+)\\\":/gm)].map(x=>x[1]);const back=[...rs.matchAll(/\\\"([^\\\"]+)\\\"/g)].map(x=>x[1]);if(JSON.stringify(front)!==JSON.stringify(back)){console.error({front,back});process.exit(1)}console.log('Category icon registries match:',front.join(', '))\"",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "qrcode": "^1.5.4",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "agentation": "^3.0.2",
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  }
}
