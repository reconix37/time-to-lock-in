# PRD: Time To Lock In — Desktop Activity Tracker (v2, готова к разработке)

> Название утверждено: **Time To Lock In** («время врубиться»). App id: `com.timetolockin.app`, репо: `reconix37/time-to-lock-in`, asset: `time-to-lock-in-setup-<version>.exe`.
> Grill-me (2026-08-10) → PRD v1 → 6-раундовая дискуссия с Codex (gpt-5.6-sol) → v2 + аудит раунда 7 (21 закрытая дыра).

## Problem

Человек проводит за компьютером 8-12 часов в день и не знает, куда реально уходит время. Ручное логирование умирает за неделю, готовые трекеры (RescueTime, ActivityWatch) — серые, негеймифицированные, с чужими категориями. Хочется автоматическую приватную штуку, которая сама знает, что Steam — это брейнрот, мотивирует делать дела и даёт красивый артефакт, который не стыдно кинуть кентам.

## Product Loop (главный цикл)

`автонаблюдение → классификация → зачёт дня → терминальная Печать → картинка кенту → локальный челлендж → установка без регистрации`

## Success Criteria (все бинарные, проверяемые за день)

- [ ] **First-run (60 сек)**: за первые 60 секунд после первого запуска юзер видит реально обнаруженное приложение, принимает useful-цель и waste-лимит, классифицирует один настоящий сегмент и видит его на живом дашборде. Установка расширения браузера НЕ блокирует завершение онбординга
- [ ] 10 минут в Steam + 10 минут кода → дашборд показывает два сегмента в правильных категориях
- [ ] Создание категории «Brainrot» + правило `steam.exe → Brainrot` за < 1 минуту, работает сразу (без рестарта)
- [ ] В Chrome: ютуб и гугл-док в разных вкладках — статистика их различает (через расширение)
- [ ] Public XP капает (1 XP/мин useful), ранг растёт по мемной шкале, хитмап недели заполняется
- [ ] Переклассификация сегмента (Brainrot → Work) пересчитывает XP, зачёт дня и графики задним числом
- [ ] В конце дня доступна «Печать дня» (терминальный лог-вердикт) и PNG-карточка для кента
- [ ] Челлендж: сгенерированный код `TF-184-43-60` импортируется на чистой машине, цели сохраняются локально
- [ ] После перезагрузки ПК трекер сам поднялся, данные целы (потеря максимум 5 сек)

## Scope

### In scope (v1)
- Вотчер активного окна (Windows, GetForegroundWindow, поллинг 5 сек). **TimeForge игнорирует собственные окна**
- Расширение Chrome/Edge (MV3): шлёт домен + тайтл вкладки в локальный HTTP-эндпоинт
- **Онбординг 60 сек** (см. «First-run» ниже): локальность → готовые категории → расширение (пропускаемое) → живой дашборд
- Кастомные категории: CRUD, цвет, иконка, kind (useful / neutral / waste). Дефолт при первом запуске: Work / Chill / Brainrot — как готовый старт, а не форма настройки
- Правила классификации: exe / процесс / подстрока тайтла / домен → категория, приоритет правил
- **Умные подсказки правил**: вечером «1.2 ч в Figma — это Work? Создать правило?» — одно нажатие
- Переклассификация задним числом (клик по сегменту → сменить категорию → пересчёт)
- Локальная БД SQLite; никаких внешних вызовов
- Дашборд: **живой** (текущее приложение, растущий сегмент таймлайна), сегодня (кольцо категорий + кликабельный таймлайн дня), тренды 7/30, топ категорий
- **Графики v1** (спека раунда 4, Codex): три bullet-бара зачёта дня (риска порога; waste в обратной логике — приближение к лимиту), накопительные линии дня (useful/waste по часам + линии цели/лимита), горизонтальный рейтинг приложений (клик подсвечивает сегменты на таймлайне), стековые столбцы по дням (7/30, useful/neutral/waste + линии цели через все дни), календарный хитмап (5 дискретных уровней через color-mix, useful или waste-режим; «сегодня» — контур, зачтённый день — точка --ok, цвет кодирует величину, статус — отдельно), тренд useful за 30 дней со скользящим средним 7д
- **Две темы**: светлая Rosé Pine Dawn (дефолт) + тёмная Rosé Pine Moon, переключатель в шапке, сохранение в настройках
- **Зачтённый день**: день зачтён ⇔ `useful >= цель` И `waste <= лимит` И `observed >= 60 мин`. Цели настраиваются в онбординге. Достижение выдаётся после завершения дня; переклассификация пересчитывает задним числом; пропуск дня не отнимает заработанное
- **Печать дня (единый компонент)**: дневной вердикт в форме терминального агрегированного лога — три условия дня, useful/neutral/waste, Public XP, ранг, «сожжено ₽» (opt-in по часовой ставке). Используется: первое открытие после дня, история по дням, источник PNG
- **Public XP**: = полные минуты в useful. Единая неизменяемая формула. Переклассификация пересчитывает. Личные множители (`goal_multiplier`) — только для локальных целей, НЕ влияют на XP/ранг/карточки
- **8 мемных рангов без деградации** (по накопленному за всё время Public XP):

  | Ранг | Min Public XP | ≈ полезного времени |
  |---|---|---|
  | Хомяк | 0 | 0 |
  | Стажёр | 500 | 8.3 ч |
  | Кодер | 2 000 | 33 ч |
  | Фокусник | 5 000 | 83 ч |
  | Тайм-ниндзя | 12 000 | 200 ч |
  | Киберсамурай | 25 000 | 417 ч |
  | Архитектор времени | 50 000 | 833 ч |
  | Повелитель времени | 100 000 | 1 667 ч |

  Прогресс до следующего ранга показывается на мини-окне и в шапке. Пороги — конфиг, но дефолт каноничен (не менять в v1)
- **Share v1 — ровно 3 PNG-артефакта** (никакой сетевой отправки, юзер сам кидает в мессенджер):
  1. `Печать дня` (вертикальный PNG: условия дня, статус «ДЕНЬ СИЛЫ»/«ДЕНЬ ЗОМБИ», ранг, бары категорий, burned ₽, «Tracked locally. No screenshots.», QR/ссылка на установку). Кнопка «Скинуть Печать дня», не «Export»
  2. `Неделя / Rank` (7 печатей, зачтено 5/7, useful/waste за неделю, XP, ранг, самый сильный день, «слито N рабочих дней», ₽ opt-in)
  3. `Челлендж` — «Побей мой день»: цель из результата отправителя, код `TF-<useful>-<waste>-<observed>` (только числа, без личности), QR на установку. Импорт кода на first-run → локальные цели → `CHALLENGE PASSED/FAILED` в конце дня
- Трей: живой счётчик useful-времени, ховер — раскладка дня, клик — перекинуть текущее окно в другую категорию; меню: открыть / пауза / выход; автозапуск через плагин
- **Кенты поюзают (требование юзера)**: установщик (NSIS .exe) + стабильная страница релиза с QR — кент ставит себе такой же локальный инстанс без регистрации. Сравнение и споры — через карточки и челлендж-коды между инстансами. Никакого сервера: каждый трекает своё локально
- **Мини-окно (компактный виджет)**: default 300×228, bounded resizable (min 300×228, max 480×340), compact/expanded (expanded ≥420×300: 12h стек + rank progress), NOW-плашка с цветом категории. drag-строка; крупно — observed за текущий локальный день с подписью "Учтено сегодня"; однострочный статус условий зачёта; три бара useful/waste/neutral, где useful относительно цели, waste — относительно лимита, neutral — абсолютным временем; компактные ранг+XP; кнопки Пауза/Дашборд/pin. Смена app/title/domain начинает новый технический сегмент, но не сбрасывает крупный дневной счётчик. Трей: ЛКМ → мини, двойной клик/меню → дашборд, клик по свободной области мини → дашборд; открытие дашборда скрывает мини. Frameless, НЕ transparent. Режим «только трей» запоминается; после онбординга старт tray-only. X окна = в трей (трекинг продолжается); Exit в меню трея = полное завершение (вотчер останавливается, автозапуск поднимет при следующем входе). Иерархия: трей сообщает, мини показывает ход текущего дня и управляет текущим состоянием трекинга, дашборд объясняет день, Exit прекращает наблюдение

### OUT of scope (v1 — НЕ строить)
- Облако, синк, телефон, веб-версия, аккаунты, мультиюзер (кенты — через share-артефакты, v1.5+ для реальных дуэлей)
- macOS / Linux
- AI-инсайты «почему я так живу» (v1.2+)
- Блокировка сайтов/приложений, Pomodoro-таймер, напоминания
- Анализ содержимого окон (скриншоты, OCR)
- Многослайдовый сюжетный Wrapped (мемные номинации, анимация) — v1.1; в v1 только недельная карточка
- Соц-механики: лидерборды, недельные дуэли, совместные цели, инвайты

## First-run (60 секунд, пошагово)

- **0–5с**: запуск в трее, экран «Всё хранится локально», статус «Трекинг запущен», кнопка «Продолжить»
- **5–15с**: экран категорий с готовыми Work / Chill / Brainrot + кнопка «Оставить и начать» (изменение названий — опционально). Цели: useful-цель и waste-лимит (дефолты 120/60)
- **15–30с**: «Установить расширение Chrome/Edge» или «Пропустить». После установки — зелёный статус «Браузер подключён»
- **30–60с**: живой дашборд: текущее приложение, первый растущий сегмент, 0 XP, ранг «Хомяк», подсказка «Поработай минуту — увидишь первую статистику»
- Нулевой дашборд НЕ является первым экраном. Установка расширения — первая post-onboarding задача

## Constraints

- Windows 10/11 (Даня: Win10 19045), Chrome или Edge
- Tauri v2 (Rust backend + React/TS frontend), `tauri-plugin-autostart`, `tauri-plugin-sql`
- Rust-часть минимальна: вотчер (windows crate) + HTTP-listener (axum); логика в TS
- Расширение: Manifest V3, пишет ТОЛЬКО в localhost вотчер (не в БД напрямую)
- SQLite локально: `%APPDATA%/timeforge/`
- Ноль телеметрии, ноль внешних запросов. Приватность = фича. На карточках: «Tracked locally. No screenshots.»
- Производительность: вотчер < 1% CPU, память < 200 MB
- Public XP: единая формула, никаких множителей; `goal_multiplier` в схеме вместо `exp_multiplier`
- Шрифты/токены — строго по DESIGN.md (Rosé Pine Dawn)

## Edge Cases & Risks

- **Idle** (нет ввода > 5 мин, GetLastInputInfo): сегмент помечается `away`, в категории/observed/XP не идёт. Порог настраивается. **Медиа-исключение**: если в тайтле активного окна/вкладки есть медиа-маркер (`▶`, `(playing)` — YouTube/Spotify ставят при воспроизведении), idle ОТМЕНЯЕТСЯ — просмотр не списывается в away, трекается как обычно (иначе ютуб-марафон без ввода уходил бы в «перерыв» и статистика врала в пользу юзера). На мини-окне при AFK пульс меняется на янтарный
- **Лок-скрин / UAC / LockApp.exe**: фильтруется как `away`
- **Игры fullscreen**: GetForegroundWindow в большинстве случаев работает; погрешность принимаем
- **Браузер без расширения**: fallback на тайтл окна (правила по подстроке). Работает, но грубее
- **Расширение перезапустилось**: retry-коннект к localhost каждые 30 сек
- **Выключение ПК посреди сегмента / любое состояние**: checkpoint активного сегмента КАЖДЫЕ 5 сек (тот же тик поллинга) + flush при смене окна, pause, exit и shutdown. Гарантия потери данных — **≤ 5 сек, единая везде** (Success Criteria и Edge Cases не противоречат)
- **Переклассификация**: каскадный пересчёт XP / зачёта дня / daily_stats для всех затронутых дней — одним запросом агрегации
- **Нет правил для процесса**: `Uncategorized` — виден на дашборде, в observed/XP не идёт (observed = useful+neutral+waste только)
- **DST / смена часового пояса**: агрегация по локальной дате, погрешность допустима
- **Шумный день**: Печать дня — агрегированный лог, НЕ перечень каждого переключения окна (иначе сотни строк)
- **Денежная строка**: только при заданной часовой ставке (opt-in)
- **QR/ссылка на установку**: нужен стабильный URL релиза (TBD — GitHub Releases или свой хостинг)

## Architecture (rough)

```
timeforge/
├─ src-tauri/            # Rust
│  ├─ src/watcher.rs     # поллинг GetForegroundWindow 5с + GetLastInputInfo (idle)
│  ├─ src/http.rs        # axum: POST /event {app, title, domain} от расширения
│  └─ main.rs            # трей (счётчик useful), автозапуск, команды
├─ src/                  # React + TS
│  ├─ pages/Onboarding.tsx   # 60-сек флоу: локально → категории/цели → расширение → live
│  ├─ pages/Dashboard.tsx    # живой дашборд: текущее окно, кольцо, таймлайн
│  ├─ pages/Heatmap.tsx      # хитмап недели/месяца
│  ├─ pages/Trends.tsx       # тренды 7/30, топ категорий
│  ├─ pages/Categories.tsx   # CRUD категорий и правил (+ умные подсказки)
│  ├─ pages/History.tsx      # переклассификация, зачтённые дни, Печать дня
│  └─ lib/share.ts           # рендер 3 PNG-карточек + код челленджа
├─ extension/            # MV3: chrome.tabs → POST localhost
└─ docs/prds/
```

- **SQLite (source of truth — миграции):**
  - `categories(id, name, color, icon, kind[useful|neutral|waste], goal_multiplier, created_at)`
  - `rules(id, match_type[exe|title|domain], pattern, category_id, priority)`
  - `segments(id, ts_start, ts_end, app, window_title, domain, category_id, status[active|idle|away|paused])`
  - `daily_stats(date, category_id, duration_ms, xp)` — агрегат хитмапа/зачёта
  - `settings(key, value)` — цели (useful_goal, waste_limit), hourly_rate (nullable), observed_min
- **Механика сегментов (канон):** сегмент = непрерывное время в одном состоянии (app + window_title + domain). Каждые 5 сек вотчер читает активное окно; состояние не изменилось → +5 сек в памяти (БД не трогается); изменилось → запись сегмента + старт нового. Закрытие сегмента: смена состояния, idle (status=away), пауза (status=paused, новые не создаются), exit/shutdown (flush, потеря ≤ 5 сек). Сегменты НЕ склеиваются (A→B→A = три сегмента); группировка — только на отображении. category_id проставляется правилами при записи; переклассификация = смена category_id + пересчёт daily_stats
- **Нормализация тайтла (канон v1.0.1):** перед построением состояния window_title и event.title НОРМАЛИЗУЮТСЯ — срезаются волатильные суффиксы (`(N)`, `- N`, `(N%)`). Без этого живые тайтлы (Telegram «AI діч - (34662)» — номер чата в скобках) рвут сегмент каждый тик и плодят 0-минутный флуд. Сегмент рвётся по смене процесса/домена/нормализованного тайтла, а не по любой вибрации заголовка
- **Title-правила (канон v0.2.4):** матчатся без учёта регистра по токенам — все значимые слова паттерна должны присутствовать в названии. Номер серии и стоп-слова игнорируются, номер сезона остаётся значимым: серии одного сезона подхватываются одним правилом. Приоритет правил: `priority DESC`, затем `domain > title > exe`
- **Sentinel «Без категории» (канон v1.0.1):** категория id=0 «Без категории» (kind=neutral, серый) существует всегда; сегменты без правил классифицируются в неё, попадают в Нейтральное и observed (LEFT JOIN, не INNER). Без этого uncategorized выпадает из агрегаций и «Баланс» показывает 0 при живых данных
- Ранг = пороги по накопленному Public XP (8 мемных, без деградации). Зачёт дня — по правилу трёх условий
- Челлендж: код `TF-<useful>-<waste>-<observed>` — чистая сериализация трёх чисел, без сервера

## Localhost API расширения (контракт)

- **Bind**: только `127.0.0.1`, порт фиксированный `43110` (не слушать на всех интерфейсах)
- **Auth**: per-install token (генерируется при первом запуске, хранится в settings; расширение получает его на онбординге и хранит в chrome.storage.local)
- **Origin check**: сервер принимает только `Origin: chrome-extension://<id>` (id фиксирован в манифесте v1)
- **Endpoint**: `POST /event` (всегда `Content-Type: application/json`)
- **JSON-схема**:
  ```json
  {
    "ts": 1755000000000,        // epoch ms, обязателен
    "domain": "youtube.com",     // <= 253 симв, может отсутствовать (не-браузерные окна не шлются)
    "title": "YouTube - Chrome", // <= 500 симв, обязателен
    "url": "https://..."         // <= 2048 симв, опционально (для правил по точному URL)
  }
  ```
- **Idempotency**: ключ = sha256(ts + title + domain); дубликаты игнорируются. Расширение делает retry до 3 раз с экспоненциальной задержкой при 5xx
- **Расширение НЕ пишет в БД** — только POST; классификацию и запись делает вотчер

## Аудит раунда 7 — закрытые решения (все дыры перед разработкой)

| # | Область | Решение (канон) |
|---|---|---|
| 1 | Checkpoint / crash recovery | «В памяти» = только в пределах 5-сек окна. Каждый тик: `UPDATE segments SET ts_end, duration WHERE status='active'` (checkpoint в БД). При старте: все висящие `status='active'` закрываются с `ts_end = последний checkpoint` → статус `crashed`. Потеря ≤ 5 сек |
| 2 | Single-instance | `tauri-plugin-single-instance`: второй запуск фокусирует существующее окно; вотчер всегда один. Mutex по имени приложения |
| 3 | Время / timezone / DST | Все ts — epoch ms (UTC). Агрегация по локальной дате: `local_date` вычисляется при записи в daily_stats по локальному offset. DST-сдвиг внутри дня — допустимая погрешность, не обрабатывается |
| 4 | Browser-event fusion | Событие расширения (`{ts, domain, title}`) — источник правды для браузерных сегментов: поллинг видит chrome.exe/msedge.exe + есть событие расширения младше 10 сек → домен/тайтл из события; иначе — тайтл окна. Расширение не пишет в БД, вотчер фьюзит |
| 5 | Pairing / CORS / Edge ID | Token показывается в онбординге, вводится в расширении один раз (chrome.storage.local). Origin-check принимает ID Chrome И Edge (оба ключа в конфиге; Edge ID известен после первой сборки). `127.0.0.1:43110`, без CORS wildcard |
| 6 | URL privacy / схема | **url в v1 не хранится вовсе** — только domain + title. Правила по точному URL — out of scope (приватность > точность) |
| 7 | Classification precedence | Фикс: (1) сортировка по `priority` DESC, (2) при равном — специфичность `domain > title > exe`, (3) первый подходящий. Все матчи case-insensitive (lowercase). exe — точное/префикс, title/domain — contains |
| 8 | Исторические цели / XP | Переклассификация → пересчёт daily_stats за затронутые дни; lifetime XP = сумма daily_stats; ранг пересчитывается на лету. **Цели исторические**: изменение useful_goal/waste_limit применяется к дням С даты изменения, прошлая зачтённость не пересчитывается |
| 9 | Lifecycle дня / челленджа | Зачтённость вычисляется: конец дня (23:59) или первое открытие на следующий день; переклассификация позже → пересчёт по целям того дня. Челлендж-код: `^TF-(\d+)-(\d+)-(\d+)$`, невалидный формат → ошибка ввода, импорт только цели |
| 10 | PNG renderer | Рендер в JS через canvas (бары/текст простые, без html2canvas) → dataURL → save через dialog plugin. Размер Печати дня: 1080×1350 (4:5, Telegram). QR — JS-библиотека qrcode, офлайн |
| 11 | Стабильный QR URL | Хостинг: GitHub Releases (reconix37/time-to-lock-in), asset `time-to-lock-in-setup-<version>.exe`, ссылка latest → https://github.com/reconix37/time-to-lock-in/releases/latest. QR ведёт туда |
| 12 | Онбординг | Показывается в главном окне; трекинг работает ДО и ВО ВРЕМЯ онбординга (данные копятся с первого запуска). Расширение пропускаемо; повторно — пункт в настройках «Подключить браузер». После завершения онбординг не показывается; «Пройти заново» в настройках (данные не трогает) |
| 13 | БД: миграции / WAL / рост | Миграции: `migrations/001_init.sql...`, применяются при старте в транзакции, `PRAGMA user_version`. `journal_mode=WAL`, `busy_timeout=5000`. Ротация сегментов — v1.1 (SQLite тянет ~2-4М строк/год); в v1 retention не нужен |
| 14 | Mini-window geometry | Default 300×228, bounded resizable (min 300×228, max 480×340), compact/expanded (expanded ≥420×300: 12h стек + rank progress), NOW-плашка с цветом категории; позиция и размер в settings, при старте — clamp к рабочей области монитора. always-on-top только по pin |
| 15 | Терминальные токены Dawn | `--term-*` определены в ОБЕИХ темах (Dawn: #2a2534 / #f4ede8 / #797593) — поправлено в DESIGN.md |
| 16 | Язык UI | v1 — русский. Все строки в i18n-слое (ключи), en — v1.1 (кенты) |
| 17 | Uninstall | NSIS: данные НЕ удаляются по умолчанию; чекбокс «Удалить локальные данные» в uninstaller, осознанное действие |
| 18 | Signing / SmartScreen | v1 без code-signing. SmartScreen warning — известный риск; на release page инструкция «More info → Run anyway». Решение принято осознанно |
| 19 | Reminders / подсказки правил | Только in-app карточка на дашборде (при открытом окне). Никаких OS-toast и расписания |
| 20 | Naming | **РЕШЕНО**: Time To Lock In. App id `com.timetolockin.app`, repo `reconix37/time-to-lock-in`, asset `time-to-lock-in-setup-<version>.exe`, mutex/extension/PNG — от этого имени. Зашито в доках до старта кода |
| 21 | Normative sources | Норматив: PRD + DESIGN.md + контракты миграций/API. Prior art (WakaTime, ActivityWatch…) — НЕ норматив, поведение не заимствуется без описания |

**Вердикт аудита: после секции выше — ДОКА ГОТОВА к разработке.**

## Дискуссия с Codex (gpt-5.6-sol, 3 раунда) — зафиксированный консенсус

1. **Public XP отдельно от личных целей**: единая формула 1 XP/мин useful; множители только для локальных целей (`goal_multiplier`); стрик-бонус ×1.5 удалён
2. **Стрик «чистого времени» заменён на «Зачтённый день»** (три условия) — абсолютный стрик мотивировал бы переименовывать категории
3. **Печать дня = терминальный лог + вердикт + деньги** (opt-in) — один компонент вместо трёх функций
4. **First-run зафиксирован как проверяемый сценарий** (60 сек, см. Success Criteria)
5. **Share v1 = 3 артефакта** (Печать дня / Неделя / Челлендж с кодом) — вирусность через мессенджеры без аккаунтов; Wrapped multi-slide — в бэклог
6. **Ранги мемные, без деградации** — «Киберсамурай» продаёт лучше «GOLD III»; деградация только через дневные вердикты

## Конфетки (креативная сессия, 2 субагента + Codex)

- **Умные подсказки правил** — приложение само умнеет, убивает боль настройки (v1)
- **Деньги впустую** — waste × часовая ставка = «сожжено ₽», строчка на Печати дня (v1, opt-in)
- **Живой трей** — счётчик useful в иконке, переклассификация с трея (v1)
- **Челлендж «Побей мой день»** — локальный код цели, асинхронный спор без сервера (v1)
- **Недельная rank-карточка** — 7 печатей, зачтено 5/7, «слито N рабочих дней» (v1)

### Бэклог v1.1+ (не строим в v1)
- Многослайдовый сюжетный Wrapped (мемные номинации, анимация)
- Босс недели (HP = минуты waste прошлой недели, урон = useful-минуты)
- Комбо-мультипликатор (×1 → ×1.5 за 25-мин блоки фокуса подряд)
- Страховка стрика (щит за фокус-часы)
- Золотой час (случайный час дня ×2, вспышка в трее)
- Лутбокс фокуса (титулы, скины темы за каждые 3ч useful)
- Скрытые ачивки-мемы («Ночной страж» — 5ч YouTube после 2 ночи)
- Маскот-компаньон в трее (ест фокус-часы)
- Проверка часовым (тост «Ты в Brainrot 47 минут. Продолжаем?» + EXP за осознанность)
- Инсайты: Зона силы, детект потока, фрагментация дня, хитмап дёрганий
- Квесты из данных, Стена позора + Зал славы, Пятница-зарплата, Prestige (+5% навсегда)
- Мультиюзер-дуэли для кентов (когда сам поюзает)

## Нейминг — РЕШЕНО

**Time To Lock In** («время врубиться»). App id: `com.timetolockin.app` · репо: `reconix37/time-to-lock-in` · asset: `time-to-lock-in-setup-<version>.exe` · QR: releases/latest. Краткая форма в UI: **TTLI** (лого, трей).

## References

- Prior art: **WakaTime** (хитмап + отчёты), **ActivityWatch** (open-source вотчер), **RescueTime** (категории), **Habitica / Duolingo** (геймификация), **HabitForge** (квесты/арена — механики в v1.1+)
- Дизайн: `/root/timeforge/DESIGN.md` (Rosé Pine Dawn, Linear-принципы)
