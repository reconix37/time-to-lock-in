# v0.2.14 — Eduard feedback round 2 + full rework (final verdict)

Дата: 2026-08-15. Источники: bugs.pdf (ночной репорт 13.08 02:01–03:16), ауцпцп.pdf (13.08 13:35–13:43 + 15.08 12:46–12:47), PS Дани (видео в брейк), аудиты 2 субагентов (скрины: 17 проблем; код: CategoryManager 577 стр), грилл-решения Дани, дебат Codex (2 раунда).

Статус: вердикт согласован, реализация НЕ начата. Слайсы в порядке работ.

## Канон (не менять)
- kind/score: kind → цели/XP/зачёт, score → аналитика (v0.2.9-categories.md:31)
- Размеры mini: 300×228…480×340 (tray.rs:13, tauri.conf.json:23); 240×170 не возвращать
- Секунды не вводить; длительности часы+минуты
- Код в TTLI ТОЛЬКО Codex (Hermes не патчит)

## Слайс 0 — correctness (M)
| Решение | Файлы |
|---|---|
| C1: Chromium media fallback: fresh event → `event.media_playing OR marker(event.title) OR marker(window_title)`; stale/no event → `marker(window_title)`. 90s age оставить | watcher.rs:84,358 |
| A9: Windows-helper `enforce_mini_topmost`: если mini_pinned=1 → SetWindowPos(HWND_TOPMOST, SWP_NOMOVE\|SWP_NOSIZE\|SWP_NOACTIVATE) на restore/show/Focused(false). set_focus НЕ использовать | tray.rs:133,202; lib.rs:1588; Cargo.toml:35 |
| Migration 006: схлопнуть дубли правил одной категории (сохранить max priority, мин id), unique index (category_id, match_type, match_mode, pattern, case_insensitive). Один паттерн в разных категориях НЕ блокировать | migrations/006_rule_uniqueness.sql; db.rs |
| Backend anti-dup в create_rule/update_rule; frontend — ранняя подсказка | lib.rs:1003,1042 |
| Видимая версия: About с runtime package version (не хардкод), в релизе v0.2.14 | App.tsx:1528; i18n.ts:272; tauri.conf.json:4 |

Muted video = явный backlog (content script, tab.audible) — НЕ в v0.2.14, решение владельца.

## Слайс 1 — Categories & Rules (L) — главный блокер
Раскладка: отдельный resizable-диалог `min(1160px, 100vw-48px)`, `resize:both`, свои скроллбары. ≥1100px: `300–320px | minmax(0,1fr)`; <1100px: одна колонка, tree 240–280px. All activity — полоса на всю ширину. CategoryManager ВЫНОСИТСЯ из .settings-scroll (App.tsx:1318).

| Решение | Файлы |
|---|---|
| Вернуть master-detail (v0.2.13 регресс: 1 колонка + overflow:hidden) | App.css:323 |
| Один интерфейс правил (убрать параллель «Categories vs Rules») | CategoryManager.tsx:545 |
| Rule card: «Приложение содержит „youtube“ → Развлечения», бейджи, полный pattern через Изменить | CategoryManager.tsx:479; App.css:390 |
| Человеческая форма: «Если [источник] [содержит] [значение], отнести к [категории]», поле «Что искать» + source-specific placeholder | CategoryManager.tsx:452; i18n.ts:190 |
| Regex+Case в настоящее «Дополнительно» (toggle-rows с пояснениями); regex меняет оператор на «соответствует выражению» | CategoryManager.tsx:460,469 |
| Оживить manager.ruleHint (под полем) и manager.exampleStudy (placeholder названия категории) | i18n.ts:149,167 |
| Фикс openRules(0): «Нужно разобрать» → форма с category_id=0, обязательный выбор, НИКОГДА manageable[0] | CategoryManager.tsx:413 |
| «Правила» → список, форма только по «+ Добавить правило» (убрать creatingRule=true из openRules) | CategoryManager.tsx:413,538 |
| Anti-dup signature + category_id и case_insensitive; ошибка без «(приоритет N)» | CategoryManager.tsx:90,278 |
| Скрыть числовой Priority: «Раньше/Позже» в «Все правила», tie-break человеческими словами | CategoryManager.tsx:139,364 |
| Discriminated state (category/rules/allRules/newCategory/newRule) вместо 5 пересекающихся стейтов | CategoryManager.tsx:114 |
| Детерминированный reload/delete (сохранять выбранный, потом parent→sibling→«Нужно разобрать») | CategoryManager.tsx:181 |
| «← Категории» на узком layout | CategoryManager.tsx:551 |
| Цели ≥32×32, строки ≥48, select/input 40px; 🗑 → текст «Удалить» | CategoryManager.tsx:496,564; App.css:279 |
| Убрать SMART/REGEX/WEB/TITLE/APP → «Приложение/Сайт/Заголовок окна/Обычный поиск/Регулярное выражение» | CategoryManager.tsx:96,540 |
| «Перекрасить всё» убрать из toolbar; в «Все правила → Дополнительно» число сегментов; ручные — off по умолчанию | CategoryManager.tsx:394,554 |
| Очистить мёртвые CSS/i18n (19 ключей; 2 оживить, остальные удалить в 3 локалях) | App.css:192,270; i18n.ts:142 |

## Слайс 2a — lifecycle mini (M)
| Решение | Файлы |
|---|---|
| Тултипы (title + aria-label) на corner-lock, AOT, settings, minimize, hide, status dot | MiniView.tsx:493,507 |
| Corner-pin = startup policy: mini показывается при старте если mini_corner валиден, даже при mini_visible=0/tray-only | tray.rs:133 |
| Dashboard НЕ скрывает mini (убрать mini.hide() из show_dashboard) | tray.rs:163 |
| `_` → minimize, `×` → hide в tray + mini_visible=0; show_mini сначала unminimize; ничего не завершает процесс | MiniView.tsx:506; tray.rs:191; lib.rs:1577 |
| В mini settings popover текстовые «Свернуть»/«Скрыть в трей» | MiniView.tsx:590 |
| Opacity 60–100% шаг 5, default 100, mini_opacity в settings, Win32 WS_EX_LAYERED/SetLayeredWindowAttributes (windows crate, без новых зависимостей) | tray.rs:202; db.rs:274 |
| Main Settings секция mini: mode, text size, NOW privacy, launch, opacity, показать/свернуть/скрыть | App.tsx:1383; MiniView.tsx:594 |
| Stop убрать из footer → tray (labels: Stop tracking/Resume) + блок «Отслеживание» в Settings | MiniView.tsx:572; tray.rs:41; App.tsx:970 |
| Status dot tooltip: «Отслеживание включено/Перерыв/Остановлено» | MiniView.tsx:457; App.css:850 |

## Слайс 2b — Score-first mini (L)
Compact 300×228 / Large 340×252: hero = SCORE ЗА СЕГОДНЯ (−13.7) + productive% + Top 3 категории (dot·путь·duration) + NOW + «Учтено сегодня» + Dashboard. Rank/goal-бары в compact НЕ показывать. Математика Large: 250 ≤ 252.
Expanded 420×320: Score + Top 4 + NOW + 3 goal-бара + «Учтено · Ранг: Хомяк · 42 XP» (подпись!). Математика Large: 310 ≤ 320.

| Решение | Файлы |
|---|---|
| Hero = Score (заменить observedToday) | MiniView.tsx:121,543; App.css:855 |
| Все rows → minmax(min,auto) по канону v0.2.11 | App.css:786 |
| Large очевидный: body +2px, score hero +8–10%, Top-строки 20px, авто-рост до 340×252 | MiniView.tsx:150; App.css:813 |
| formatDuration → общий helper (dashboard/mini/preview) | MiniView.tsx:59; App.tsx:1000 |
| Harness: mock get_today_scoring, opacity, window commands; убрать мёртвый mini_hourly | mini-ui-harness.ts |

## Общий дизайн: добавить / убрать
Добавить: человеческие предложения правил, source-placeholders, пояснения kind/score, один tooltip-механизм + видимый help (не hover-only), полные пути через title, текстовые Изменить/Удалить/Раньше/Позже, «Score = влияние × время» в ScorePanel, About → v0.2.14.
Убрать: SMART/REGEX/WEB/TITLE/APP, числовой Priority из текста, пустую Advanced-коробку, emoji-корзины и микро-кнопки, inline-edit каждой rule-card, CategoryManager из settings-scroll, Stop из mini footer, Tracked today hero, неподписанный «Хомяк · 42», пустые «Top productive: —» (ScorePanel.tsx:45 — не рендерить пустое, общий empty-state), старые CSS/i18n.

## Порядок работ
1. Slice 0 — correctness (media fallback, migration/anti-dup, TOPMOST, version)
2. Slice 1 — Categories & Rules (dialog, master-detail, cards, nav, replay, cleanup)
3. Slice 2a — mini lifecycle (tooltips, startup, dashboard, min/hide, opacity, Stop)
4. Slice 2b — Score-first mini (hero, Top 3/4, math, Large, harness)
5. Slice 3 — release gate (dashboard score cleanup, i18n audit, smoke, CI, таблица Эдуарду)

Категории перед mini — главный блокирующий feedback.

## Release gate
- npm run build; frontend tests (classification/i18n/mini verdict)
- Harness: RU/UA/EN × Normal/Large × 300×228/340×252/420×320/480×340 = 24 сценария; no scrollbars/clipping/overlap
- cargo fmt из src-tauri; Rust compile/test только Windows CI; один фильтр на команду
- Migration test: схлопывание дублей, разный pattern разных категорий, unique guard
- Media tests: fresh audible, fresh marker, stale window marker, stale no marker, forced-away
- Ручная Windows-матрица: Win10 19045/Win11, scale 100/125/150%, Dawn/Moon, main 1280×800/960×640, AOT vs Explorer/Chrome, corner+reboot, dashboard+mini, min/hide, opacity 60/80/100 после рестарта, видео после idle, rule из «Нужно разобрать» без выбора категории невозможен, rule применяется ≤5с

## Ответ Эдуарду (черновик)
Признать: «v0.2.13 оказался визуальной перестановкой и не закрыл старые функциональные баги. Собрали оба PDF в один проверяемый список». Таблица «твой пункт → где сейчас → что будет в v0.2.14» (14 строк, см. полный текст дебата). Перед отправкой — ручной прогон сценариев + в Settings → About явно `TTLI v0.2.14`.

## Финальный вердикт Codex
«v0.2.14 принимается только как единый исправительный релиз: сначала достоверность данных и Windows lifecycle, затем полный master-detail реворк Categories & Rules, затем Score-first mini. Нельзя выпускать только визуальную часть. Все пункты двух PDF либо имеют проверяемое исправление, либо отмечены закрытыми с regression-тестом; единственное исключение — muted-video (backlog по решению владельца). Release gate — Windows CI, 24 mini-сценария, ручная матрица, таблица жалоба→фикс→smoke-test с видимой версией v0.2.14.»
